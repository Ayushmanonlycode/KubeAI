apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: graph-data
  namespace: ai-observability
  labels:
    app: ai-observability-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
