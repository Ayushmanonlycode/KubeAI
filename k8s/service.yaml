apiVersion: v1
kind: Service
metadata:
  name: ai-observability-backend
  namespace: ai-observability
  labels:
    app: ai-observability-backend
spec:
  selector:
    app: ai-observability-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
